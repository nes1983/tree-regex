If you have write access, you can check out this repo as follows:

	git clone git@scg.unibe.ch:papers-regex-fast

If a read-only version is available ("daemon = yes" in gitosis.conf),
then it can be checked out as follows:

	git clone git://scg.unibe.ch/papers-regex-fast

